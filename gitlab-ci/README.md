# Gitlab CI Support for Carthage

This directory contains jobs that are intended to be used in Gitlab CI
pipelines to assist in using Carthage with Gitlab.

note that Carthage itself is hosted on Github and thus does not directly use these pipelines in its own build.
